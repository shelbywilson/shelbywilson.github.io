import ReactDOM from 'react-dom';
import React from 'react';
import { BrowserRouter } from "react-router-dom";

import Tunnel from '../pages/gradients/tunnels/2/Tunnel';

ReactDOM.render(
  <BrowserRouter>
    <Tunnel />
  </BrowserRouter>
  , document.getElementById('root'));